export interface Movie {
  director: string,
  genres: string[],
  cast: string[],
  id: string,
  imdb_rating: number,
  length: string,
  overview: string,
  poster: string,
  released_on: string,
  title: string
}
